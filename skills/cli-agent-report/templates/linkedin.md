# LinkedIn Post — {{CLI}}

<!-- Copy everything between the lines below into LinkedIn -->
---
If your agent uses "{{CLI}}" today, {{HOOK}}.

I ran a live evaluation of "{{CLI}}" against {{N_FINDINGS}} Critical failure modes. {{FAIL_COUNT}} score 0/3. {{PARTIAL_COUNT}} score 1–2/3. Here's what's silently breaking your agent right now:

🔴 {{SCORE_0_ISSUE_1}}

🔴 {{SCORE_0_ISSUE_2}}

⚠️ {{HIGH_IMPACT_GAP}}

<!-- Use 🔴 for score-0 failures, ⚠️ for score-1 or high-impact score-2. List all problem bullets before any fix bullets. -->

Here's how to fix each one:

✅ {{FIX_FOR_ISSUE_1}}

✅ {{FIX_FOR_ISSUE_2}}

✅ {{FIX_FOR_GAP}}

<!-- Fix lines are plain-prose patterns, never code. One sentence describing the approach. Same order as the issues above. -->

Full integration guide, runtime brief, and issues report — all in the first comment.

<!-- Optional: one @mention on its own line here, before hashtags. Tag org/company LinkedIn page not an individual. Omit entirely if handle unknown — never guess. -->

#AIAgents #{{DOMAIN_TAG}}

<!-- 2–4 hashtags total. #AIAgents plus 1–3 domain-specific. More than 4 reduces engagement. -->
---

<!-- First comment to post separately: -->
Full evaluation report: [PASTE LINK HERE]

<!-- FORMAT RULES (apply before writing content):
     - No markdown: no backticks, no **bold**. Wrap tool names in "double quotes".
     - First person: "I ran", "I evaluated" — never "we".
     - Aim 200–350 words. LinkedIn cap is ~3000 chars.
     - No "what works well" section.
     - Never guess maintainer LinkedIn handle; omit @mention if unknown.
     - Never add @mentions to any other report file. -->
