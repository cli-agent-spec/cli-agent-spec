# X Thread — {{CLI}}

<!-- Post each numbered block separately, in order -->

---
**1/**
If your agent uses `{{CLI}}` — {{HOOK}} 🧵

I ran a live evaluation: {{N_FINDINGS}} Critical failure modes against the real binary. Here's what's broken.
---

**2/**
🔴 {{SCORE_0_ISSUE_1}}
---

**3/**
🔴 {{SCORE_0_ISSUE_2}}
---

**4/**
⚠️ {{HIGH_IMPACT_GAP}}
---

**5/**
Copy these into your agent's `{{CLI}}` wrapper before shipping:

✅ {{FIX_FOR_POST_2}}
✅ {{FIX_FOR_POST_3}}
✅ {{FIX_FOR_POST_4}}
---

**6/ (closing)**
{{FAIL_COUNT}} Critical gaps at zero. {{PARTIAL_COUNT}} partial. Full integration guide + runtime brief →

[PASTE LINK HERE]

<!-- Optional: @XHandle on its own line. Omit if unknown — never guess. -->

#AIAgents
---

<!-- FORMAT RULES (apply before writing content):
     - 280 characters per post — count carefully including spaces and punctuation. Link counts ~23 chars.
     - Post 1 ends with 🧵; no link in post 1 (algorithm suppresses link-in-first-post).
     - Posts 2–4: one issue each, no fixes. Post 5: all fixes in matching order.
     - Fix lines are prose patterns, never code.
     - 🔴 = score-0, ⚠️ = score-1 or score-2 high impact.
     - 1–2 hashtags maximum in closing post only.
     - Never add @mentions to any other report file.
     - "No we evaluated" framing — start from the user's problem. -->
