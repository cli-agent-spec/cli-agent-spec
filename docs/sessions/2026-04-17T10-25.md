# Session — JSON Envelope And Streaming Clarification

**Date:** 2026-04-17 10:25 (EEST)
**Conversation ID:** (unclear)
**Workspace:** cli-agent-ergonomics
**Model:** (unclear)

---

## 1. Goals

- Explain how JSON envelopes and streaming fit together in the CLI Agent Spec
- Confirm whether the topic required spec changes or could be handled as clarification only
- Provide short wording the maintainer could reuse in discussion or review

## 2. Work Done

- Read the relevant requirement and schema docs covering envelopes, output modes, streaming, heartbeats, and step events
- Traced the relationship between `REQ-F-004`, `REQ-O-001`, `REQ-O-004`, `REQ-O-038`, `REQ-C-008`, and `ResponseEnvelope`
- Summarized the current spec behavior: `--output json` uses a final `ResponseEnvelope`, `--stream` / `--output jsonl` uses line-delimited item objects, and heartbeat lines may precede a final envelope
- Advised that no spec change was required because the behavior was already covered by existing requirements
- Drafted short clarification text and a shorter review-comment version
- Answered whether clarifications must be added to requirements and recommended against changing normative text unless implementer ambiguity is demonstrated

## 3. Decisions Made

| Decision | Rationale |
|----------|-----------|
| Treat JSON envelope and streaming as already covered by the existing spec | Existing requirements already define buffered envelope mode, streaming mode, and the heartbeat-plus-final-envelope pattern |
| Recommend explanation outside the requirements by default | The current wording appears sufficient when read together, so extra normative text would add noise unless real ambiguity is shown |
| Use `(unclear)` for conversation metadata not exposed in the thread | The skill forbids fabrication and requires explicit uncertainty when details are unavailable |

## 4. Decisions Refused / Alternatives Rejected

| Rejected Approach | Reason |
|-------------------|--------|
| Propose a spec change immediately | The maintainer indicated the topic was already present and no changes were required |
| Add requirement clarifications by default | No demonstrated contradiction or implementer-breaking ambiguity was established in this session |
| Claim that each streamed line should be wrapped in `ResponseEnvelope` | The current spec explicitly treats `jsonl` streaming as line-based objects without an envelope wrapper, except for the final envelope after heartbeat lines |

## 5. Open Issues & Next Steps

- [ ] Reuse the drafted clarification paragraph in an issue comment, review reply, or maintainer note if needed
- [ ] Add supporting explanatory docs only if the same confusion keeps recurring in reviews
- [ ] Revisit the requirements only if implementers continue to interpret the current interaction incorrectly
