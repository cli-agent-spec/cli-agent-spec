# Session — Schema Alias Compatibility Update

**Date:** 2026-04-16 17:12 (EEST)
**Conversation ID:** _(unclear)_
**Workspace:** cli-agent-ergonomics
**Model:** _(unclear)_

---

## 1. Goals

- Assess whether changing schema-discovery flag naming was worthwhile after observing an LLM guess `--print-schema` instead of implemented `--schema`
- Update the specification to support `--print-schema` as a compatibility alias while keeping `--schema` canonical
- Save a session report for the work completed

## 2. Work Done

- Reviewed the current spec usage of `--schema` across requirements, challenges, and implementer guidance
- Confirmed the canonical requirement text in `requirements/o-013-schema-output-schema-flag.md`
- Updated `requirements/o-013-schema-output-schema-flag.md` to require `--print-schema` as a compatibility alias for `--schema`
- Added acceptance criteria that `--print-schema` returns the same payload as `--schema`
- Updated `challenges/06-high-errors-and-discoverability/21-medium-schema-discoverability.md` to mention the compatibility alias in framework guidance and evaluation criteria
- Updated `IMPLEMENTING.md` to mention the alias in the implementation-path summary for `REQ-O-013`
- Ran validation checks for schema↔requirement symmetry and index completeness
- Ran a repo-wide markdown link scan and noted unrelated pre-existing broken links outside the edited files

## 3. Decisions Made

| Decision | Rationale |
|----------|-----------|
| Keep `--schema` as the canonical spelling | The spec already uses `--schema` consistently across requirements, challenges, and implementation guidance |
| Add `--print-schema` as a compatibility alias instead of renaming the feature | Real-world LLM behavior showed that some models guess `--print-schema`, so accepting it improves interoperability without large spec churn |
| Limit the edit to the requirement, motivating challenge, and implementer guide | This captures the policy change in the normative source and key supporting docs without unnecessary broad rewrites |

## 4. Decisions Refused / Alternatives Rejected

| Rejected Approach | Reason |
|-------------------|--------|
| Replace `--schema` with `--print-schema` as the new canonical flag | The existing spec is already standardized on `--schema`, so a rename would create broad documentation churn for limited benefit |
| Leave the spec unchanged after the observed failed LLM call | The observed failure provided concrete evidence that a compatibility alias would improve agent success in practice |

## 5. Open Issues & Next Steps

- [ ] Decide whether to also document a friendly unknown-flag hint such as suggesting `--schema` when callers use `--print-schema` on non-compliant tools
- [ ] Clean up the unrelated pre-existing broken markdown links reported by the repo-wide scan
- [ ] Fill in conversation metadata fields if a future workflow exposes conversation ID and model name programmatically
