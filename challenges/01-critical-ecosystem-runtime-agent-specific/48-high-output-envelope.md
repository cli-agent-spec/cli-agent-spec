# 48. Output Envelope

> **MERGED** — This challenge has been consolidated into **§2** because it describes
> a specific case of the same root problem.
>
> See: [`02-critical-output-format.md`](../04-critical-output-and-parsing/02-critical-output-format.md)
>
> **Agent workaround:** apply the §2 workaround — discover the JSON flag from `--help`, parse with the JSON extraction rule in [`triage.md`](../triage.md), branch on the envelope's `ok` field
